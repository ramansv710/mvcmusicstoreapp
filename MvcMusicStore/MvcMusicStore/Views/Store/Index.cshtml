@model IEnumerable<MvcMusicStore.Models.Genre>
@{
    ViewBag.Title = "Store";
}

<h3>Browse genres</h3>

<p>
    Select from @Model.Count()
</p>

<ul>
    @foreach (var genre in Model)
    {
        <li>@Html.ActionLink(@genre.Name, "Browse", new { @genre.Name })</li>
    }
</ul>